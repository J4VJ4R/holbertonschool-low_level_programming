#include "holberton.h"
#include <stdlib.h>
/**
 * _abs - show the absolute value of n
 * @n:int checked
 * Return: 0 on success
 */

int print_last_digit(int n)
{
        int 
		int last = _abs(n % 10);

        
        return (last);
}
