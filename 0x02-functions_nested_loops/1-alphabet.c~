#include "holberton.h"

/**
 * main - prints the alphabet in lowercase
 *
 * Return: 0 on success
 */

int print_alphabet(void)
{
	char letter = 'a';

	for (letter = 'a'; letter < 'z'; letter++)
	{
		_putchar(letter);
	}
	_putchar('\n');
	return (0);
}
