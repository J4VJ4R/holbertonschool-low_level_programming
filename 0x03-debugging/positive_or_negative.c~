#include "holberton.h"

/**
 * positive_or_negative - create a code where find a number positive or negative
 *@i:int checked
 * Return: 0 on success
 */

void positive_or_negative(int i)
{
	if(i > 0)
	{
		printf("%d is positive\n", i);
	}
	return (0);	
}
